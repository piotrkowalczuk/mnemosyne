comment:
  layout: header, diff, changes, uncovered
coverage:
  ignore:
  - mnemosyne.pb.go
  status:
    patch: false
    project:
      default:
        branches:
        - master
        target: auto